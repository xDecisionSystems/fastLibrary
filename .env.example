# MongoDB (required)
MONGO_URI=mongodb://localhost:27017
MONGO_DB=paper_library

# API server (optional — defaults shown)
API_HOST=0.0.0.0
API_PORT=8000

# Local PDF storage (optional — default shown)
PDF_DIR=/opt/paper-library/pdfs
